{
  "source": {
    "upload": {
      "name": "upload",
      "version": "1",
      "summary": "Takes a file you uploaded. Files are stored once, named by a fingerprint of their bytes, so the same file used in several pipelines is a single copy on disk.",
      "stage": "source",
      "output": "raw_file",
      "stackable": false,
      "deterministic": true,
      "cacheable": true,
      "requires": {},
      "provides": {},
      "inputs": {},
      "config_schema": {
        "properties": {
          "sha": {
            "default": "",
            "title": "Sha",
            "type": "string"
          },
          "filename": {
            "default": "",
            "title": "Filename",
            "type": "string"
          }
        },
        "title": "UploadConfig",
        "type": "object"
      },
      "learn": {}
    }
  },
  "use_case": {
    "chat": {
      "name": "chat",
      "version": "2",
      "summary": "Sends the retrieved pieces and your question to a chat model (Claude, OpenAI, or any OpenAI-compatible server), which answers using only those pieces. Every claim points at the passages it relied on, and each citation is checked against the parsed document.",
      "stage": "use_case",
      "output": "output",
      "stackable": false,
      "deterministic": false,
      "cacheable": false,
      "requires": {},
      "provides": {},
      "inputs": {
        "result": {
          "type": "retrieval_result",
          "variadic": false,
          "ambient": false,
          "required": true
        },
        "query": {
          "type": "query",
          "variadic": false,
          "ambient": true,
          "required": true
        },
        "doc": {
          "type": "parsed_doc",
          "variadic": false,
          "ambient": true,
          "required": true
        },
        "index": {
          "type": "index",
          "variadic": false,
          "ambient": true,
          "required": true
        }
      },
      "config_schema": {
        "properties": {
          "model": {
            "default": "claude-opus-5",
            "enum": [
              "claude-opus-5",
              "claude-sonnet-5",
              "claude-haiku-4-5",
              "gpt-6-astra",
              "gpt-5.6-sol",
              "gpt-5.6-luna",
              "custom"
            ],
            "title": "Model",
            "type": "string",
            "x-labels": {
              "claude-haiku-4-5": "Claude Haiku 4.5",
              "claude-opus-5": "Claude Opus 5",
              "claude-sonnet-5": "Claude Sonnet 5",
              "custom": "Custom (OpenAI-compatible)",
              "gpt-5.6-luna": "GPT-5.6 Luna",
              "gpt-5.6-sol": "GPT-5.6 Sol",
              "gpt-6-astra": "GPT-6 Astra"
            }
          },
          "custom_base_url": {
            "default": "",
            "title": "Custom Base Url",
            "type": "string",
            "x-show-when": {
              "model": "custom"
            }
          },
          "custom_model": {
            "default": "",
            "title": "Custom Model",
            "type": "string",
            "x-show-when": {
              "model": "custom"
            }
          },
          "citation_method": {
            "default": "auto",
            "enum": [
              "auto",
              "sentence_ids"
            ],
            "title": "Citation Method",
            "type": "string"
          },
          "support_threshold": {
            "default": 0.55,
            "maximum": 1,
            "minimum": 0,
            "title": "Support Threshold",
            "type": "number"
          },
          "max_chunks": {
            "default": 5,
            "minimum": 1,
            "title": "Max Chunks",
            "type": "integer"
          }
        },
        "title": "ChatConfig",
        "type": "object"
      },
      "learn": {}
    },
    "search": {
      "name": "search",
      "version": "1",
      "summary": "Shows the retrieved pieces as a ranked list with their scores and pages. No language model, no API key and no cost: you see exactly what retrieval found.",
      "stage": "use_case",
      "output": "output",
      "stackable": false,
      "deterministic": true,
      "cacheable": true,
      "requires": {},
      "provides": {},
      "inputs": {
        "result": {
          "type": "retrieval_result",
          "variadic": false,
          "ambient": false,
          "required": true
        }
      },
      "config_schema": {
        "properties": {
          "top_k": {
            "default": 5,
            "title": "Top K",
            "type": "integer"
          },
          "max_snippet_chars": {
            "default": 400,
            "title": "Max Snippet Chars",
            "type": "integer"
          }
        },
        "title": "SearchUseCaseConfig",
        "type": "object"
      },
      "learn": {}
    },
    "eval": {
      "name": "eval",
      "version": "1",
      "summary": "Checks whether the retrieved pieces contain the sentence that answers the question, and reports where it was found. No language model, no API key and no cost: it compares text with text.",
      "stage": "use_case",
      "output": "output",
      "stackable": false,
      "deterministic": true,
      "cacheable": true,
      "requires": {},
      "provides": {},
      "inputs": {
        "result": {
          "type": "retrieval_result",
          "variadic": false,
          "ambient": false,
          "required": true
        },
        "query": {
          "type": "query",
          "variadic": false,
          "ambient": true,
          "required": true
        }
      },
      "config_schema": {
        "properties": {
          "top_k": {
            "default": 5,
            "title": "Top K",
            "type": "integer"
          }
        },
        "title": "EvalConfig",
        "type": "object"
      },
      "learn": {
        "_strategy": {
          "hint": "This step marks one question right or wrong by looking for its answer sentence in the retrieved pieces.",
          "more": [
            "Every question carries the sentence in the document that answers it. A piece that contains that sentence is a hit, and the rank of the first such piece says how far down the reader would have had to look.",
            "The answer is a sentence rather than a piece number on purpose. Piece numbers change the moment you change how the document is cut, so a score built on them could never compare two settings. A sentence stays the same sentence.",
            "The comparison is strict. The sentence has to be there, give or take spacing, a word broken across a line and capital letters. Something that merely looks close counts as a miss, which is what keeps the number honest."
          ]
        },
        "top_k": {
          "hint": "Only this many of the retrieved pieces are checked, because that is what the reader would have looked at.",
          "more": [
            "An answer that sits below this line is reported as a miss. That is the point: a piece nobody reads cannot help.",
            "Raising it makes more questions count as hits, and makes the score say less about how well the pieces were ordered. Lowering it is a harder test of the ranking."
          ]
        }
      }
    }
  },
  "query": {
    "text": {
      "name": "text",
      "version": "1",
      "summary": "The question, exactly as you type it. It is a step of its own so that trying another question reruns only retrieval and what follows, reusing everything before it.",
      "stage": "query",
      "output": "query",
      "stackable": false,
      "deterministic": true,
      "cacheable": true,
      "requires": {},
      "provides": {},
      "inputs": {},
      "config_schema": {
        "properties": {
          "text": {
            "default": "",
            "title": "Text",
            "type": "string"
          },
          "gold_answer": {
            "default": "",
            "title": "Gold Answer",
            "type": "string"
          }
        },
        "title": "TextQueryConfig",
        "type": "object"
      },
      "learn": {}
    }
  },
  "parse": {
    "pdfium": {
      "name": "pdfium",
      "version": "2",
      "summary": "Reads the text stored in the PDF, in the order the PDF drew it. It cannot tell a heading from a paragraph, so every block comes out as a plain paragraph, and a scanned page with no stored text comes out empty. Fast, with no model to load.",
      "stage": "parse",
      "output": "parsed_doc",
      "stackable": false,
      "deterministic": true,
      "cacheable": true,
      "requires": {},
      "provides": {},
      "inputs": {
        "file": {
          "type": "raw_file",
          "variadic": false,
          "ambient": false,
          "required": true
        }
      },
      "config_schema": {
        "properties": {
          "mode": {
            "const": "text",
            "default": "text",
            "title": "Mode",
            "type": "string"
          },
          "join_lines": {
            "default": true,
            "title": "Join Lines",
            "type": "boolean"
          }
        },
        "title": "PdfiumConfig",
        "type": "object"
      },
      "learn": {}
    },
    "docling": {
      "name": "docling",
      "version": "1",
      "summary": "Renders each page as an image and runs a layout model that labels every region: title, section heading, list item, table, page header and footer. A reading-order model then puts the regions in sequence. Much slower than pdfium, with far more structure.",
      "stage": "parse",
      "output": "parsed_doc",
      "stackable": false,
      "deterministic": true,
      "cacheable": true,
      "requires": {},
      "provides": {},
      "inputs": {
        "file": {
          "type": "raw_file",
          "variadic": false,
          "ambient": false,
          "required": true
        }
      },
      "config_schema": {
        "properties": {
          "do_ocr": {
            "default": false,
            "description": "Read text from page images with OCR. Only needed for scanned PDFs with no text layer; it is much slower.",
            "title": "Do Ocr",
            "type": "boolean"
          },
          "do_table_structure": {
            "default": true,
            "description": "Recover rows and columns of detected tables, so a table reaches the chunks as a markdown table instead of loose text.",
            "title": "Do Table Structure",
            "type": "boolean"
          },
          "table_mode": {
            "default": "fast",
            "description": "Which table model to use. Fast is fine for simple grids; accurate handles merged cells better and takes longer.",
            "enum": [
              "fast",
              "accurate"
            ],
            "title": "Table Mode",
            "type": "string"
          }
        },
        "title": "DoclingConfig",
        "type": "object"
      },
      "learn": {}
    }
  },
  "clean": {
    "header_footer_strip": {
      "name": "header_footer_strip",
      "version": "1",
      "summary": "Finds running heads, running feet and page numbers: short blocks that sit first or last on a page and repeat across pages, or look like a page number. It checks again after each pass, because removing a page number can expose another footer behind it.",
      "stage": "clean",
      "output": "parsed_doc",
      "stackable": true,
      "deterministic": true,
      "cacheable": true,
      "requires": {},
      "provides": {},
      "inputs": {
        "doc": {
          "type": "parsed_doc",
          "variadic": false,
          "ambient": false,
          "required": true
        }
      },
      "config_schema": {
        "properties": {
          "min_page_ratio": {
            "default": 0.5,
            "description": "Fraction of pages a block must appear on, at the same page edge, to count as a running artefact.",
            "maximum": 1.0,
            "minimum": 0.0,
            "title": "Min Page Ratio",
            "type": "number"
          },
          "drop": {
            "default": true,
            "description": "Remove detected blocks from `elements`. When false they are only retyped, which already excludes them from the markdown projection while keeping them visible for inspection.",
            "title": "Drop",
            "type": "boolean"
          }
        },
        "title": "HeaderFooterStripConfig",
        "type": "object"
      },
      "learn": {}
    },
    "dedupe_blocks": {
      "name": "dedupe_blocks",
      "version": "1",
      "summary": "Removes a block whose text repeats an earlier block, keeping the first copy in reading order. Blocks are compared only with blocks of the same kind, so a heading and a paragraph with the same words are both kept.",
      "stage": "clean",
      "output": "parsed_doc",
      "stackable": true,
      "deterministic": true,
      "cacheable": true,
      "requires": {},
      "provides": {},
      "inputs": {
        "doc": {
          "type": "parsed_doc",
          "variadic": false,
          "ambient": false,
          "required": true
        }
      },
      "config_schema": {
        "properties": {
          "similarity": {
            "default": 0.95,
            "description": "Minimum difflib ratio to call two blocks duplicates. Only consulted when `scope` is 'near'.",
            "maximum": 1.0,
            "minimum": 0.0,
            "title": "Similarity",
            "type": "number"
          },
          "scope": {
            "default": "exact",
            "description": "'exact' compares normalized text; 'near' compares by similarity ratio.",
            "enum": [
              "exact",
              "near"
            ],
            "title": "Scope",
            "type": "string"
          }
        },
        "title": "DedupeBlocksConfig",
        "type": "object"
      },
      "learn": {}
    },
    "drop_matching": {
      "name": "drop_matching",
      "version": "1",
      "summary": "Removes every block whose text contains a pattern you name. Use it for boilerplate the automatic cleaners miss, such as a notice repeated in the middle of pages.",
      "stage": "clean",
      "output": "parsed_doc",
      "stackable": true,
      "deterministic": true,
      "cacheable": true,
      "requires": {},
      "provides": {},
      "inputs": {
        "doc": {
          "type": "parsed_doc",
          "variadic": false,
          "ambient": false,
          "required": true
        }
      },
      "config_schema": {
        "properties": {
          "pattern": {
            "default": "",
            "description": "Text to look for. Any block containing it is removed. Leave empty to remove nothing.",
            "title": "Pattern",
            "type": "string"
          },
          "mode": {
            "default": "contains",
            "description": "'contains' matches the pattern as plain text; 'regex' treats it as a regular expression found anywhere in the block.",
            "enum": [
              "contains",
              "regex"
            ],
            "title": "Mode",
            "type": "string"
          },
          "case_sensitive": {
            "default": false,
            "description": "Match upper and lower case exactly. Off means 'Notice' also matches 'notice'.",
            "title": "Case Sensitive",
            "type": "boolean"
          }
        },
        "title": "DropMatchingConfig",
        "type": "object"
      },
      "learn": {}
    }
  },
  "chunk": {
    "recursive_character": {
      "name": "recursive_character",
      "version": "1",
      "summary": "Cuts at paragraph breaks first. A paragraph too long for one piece is cut at line breaks, then at sentence ends, then at spaces, so each cut lands on the most natural boundary available. The parts are then packed together until a piece is full.",
      "stage": "chunk",
      "output": "chunk_set",
      "stackable": false,
      "deterministic": true,
      "cacheable": true,
      "requires": {},
      "provides": {},
      "inputs": {
        "doc": {
          "type": "parsed_doc",
          "variadic": false,
          "ambient": false,
          "required": true
        }
      },
      "config_schema": {
        "properties": {
          "chunk_size": {
            "default": 1000,
            "minimum": 1,
            "title": "Chunk Size",
            "type": "integer"
          },
          "chunk_overlap": {
            "default": 200,
            "minimum": 0,
            "title": "Chunk Overlap",
            "type": "integer"
          }
        },
        "title": "RecursiveCharacterConfig",
        "type": "object"
      },
      "learn": {
        "_strategy": {
          "hint": "This strategy cuts the text at the most natural place it can find.",
          "more": [
            "It first tries to cut between paragraphs. If a paragraph is too long, it cuts between lines, then between sentences, and only as a last resort between words.",
            "It then joins the small pieces back together until each chunk is as big as the chunk size you chose."
          ]
        },
        "chunk_size": {
          "hint": "This is the largest a chunk can be, counted in characters.",
          "more": [
            "A character is one letter, number, space or punctuation mark. Four characters are roughly one token, which is the unit language models count in.",
            "Small chunks match a question very precisely. But an answer that is longer than one chunk gets split, and each chunk carries little of the text around it.",
            "Large chunks keep more of the text together. But one useful sentence can get lost among many sentences that are not about your question."
          ]
        },
        "chunk_overlap": {
          "hint": "This is how much text each chunk repeats from the end of the chunk before it, counted in characters.",
          "more": [
            "It helps when an important sentence sits right on the border between two chunks, because the sentence then appears whole in at least one of them.",
            "This strategy only repeats whole paragraphs, lines, sentences or words. It never cuts a word in half to repeat it. So if the chunk before ends with a paragraph that is longer than the overlap, nothing is repeated.",
            "Overlap has a cost. Repeated text is stored and searched more than once. The overlap must also be smaller than the chunk size, or the chunks would never move forward through the text."
          ]
        }
      }
    },
    "markdown_header": {
      "name": "markdown_header",
      "version": "1",
      "summary": "Cuts at headings, so each piece is one section: a heading and everything under it, up to the next heading. A section too long for one piece is cut between its blocks, and only a single block that is itself too long is cut mid-text. Pieces do not overlap.",
      "stage": "chunk",
      "output": "chunk_set",
      "stackable": false,
      "deterministic": true,
      "cacheable": true,
      "requires": {},
      "provides": {},
      "inputs": {
        "doc": {
          "type": "parsed_doc",
          "variadic": false,
          "ambient": false,
          "required": true
        }
      },
      "config_schema": {
        "properties": {
          "max_tokens": {
            "default": 512,
            "minimum": 1,
            "title": "Max Tokens",
            "type": "integer"
          }
        },
        "title": "MarkdownHeaderConfig",
        "type": "object"
      },
      "learn": {
        "_strategy": {
          "hint": "This strategy cuts the document at its headings.",
          "more": [
            "Each chunk is one section, which means a heading and everything under it until the next heading. This keeps each topic together, and people often ask questions about one topic at a time.",
            "It needs a parser that recognises headings, such as Docling. If the parser found no headings, the whole document is treated as one section and is cut only by size."
          ]
        },
        "max_tokens": {
          "hint": "This is the largest a chunk can be, counted in tokens.",
          "more": [
            "Here a token is one word or one punctuation mark.",
            "If a section is longer than this, it is split between its paragraphs, lists and tables. One of these is only cut in the middle if it is longer than this limit on its own.",
            "Chunks from this strategy never overlap."
          ]
        }
      }
    },
    "token_based": {
      "name": "token_based",
      "version": "1",
      "summary": "Counts tokens and cuts each time the count is reached, wherever that falls. A token here is a word or a single punctuation mark. It ignores the structure of the text completely, which makes it a useful baseline to compare the other chunkers against.",
      "stage": "chunk",
      "output": "chunk_set",
      "stackable": false,
      "deterministic": true,
      "cacheable": true,
      "requires": {},
      "provides": {},
      "inputs": {
        "doc": {
          "type": "parsed_doc",
          "variadic": false,
          "ambient": false,
          "required": true
        }
      },
      "config_schema": {
        "properties": {
          "max_tokens": {
            "default": 512,
            "minimum": 1,
            "title": "Max Tokens",
            "type": "integer"
          },
          "overlap": {
            "default": 64,
            "minimum": 0,
            "title": "Overlap",
            "type": "integer"
          }
        },
        "title": "TokenBasedConfig",
        "type": "object"
      },
      "learn": {
        "_strategy": {
          "hint": "This strategy counts tokens and cuts every time the count reaches the limit, wherever that happens to be.",
          "more": [
            "It ignores sentences, paragraphs and headings.",
            "That makes it a useful baseline. Compare it with the other strategies to see how much it helps to cut in natural places."
          ]
        },
        "max_tokens": {
          "hint": "This is how many tokens go into each chunk.",
          "more": [
            "Here a token is one word or one punctuation mark.",
            "The cut happens exactly at this count, even in the middle of a sentence."
          ]
        },
        "overlap": {
          "hint": "This is how many tokens each chunk repeats from the end of the chunk before it.",
          "more": [
            "If a sentence is cut at a border and it is shorter than the overlap, it appears whole at the start of the next chunk.",
            "The overlap must be smaller than the max tokens."
          ]
        }
      }
    }
  },
  "index": {
    "lancedb": {
      "name": "lancedb",
      "version": "1",
      "summary": "Turns every piece into a vector (a list of numbers that captures its meaning) with an embedding model, and stores it in a LanceDB table with a keyword index over the same rows. Vector search and keyword search then read the same pieces, which is what lets hybrid search combine them fairly.",
      "stage": "index",
      "output": "index",
      "stackable": false,
      "deterministic": true,
      "cacheable": true,
      "requires": {},
      "provides": {
        "backends": [
          "dense",
          "fts"
        ]
      },
      "inputs": {
        "chunks": {
          "type": "chunk_set",
          "variadic": true,
          "ambient": false,
          "required": true
        }
      },
      "config_schema": {
        "properties": {
          "embedder": {
            "default": "qwen3-embedding-0.6b",
            "enum": [
              "qwen3-embedding-0.6b",
              "bge-small-en-v1.5",
              "fake-deterministic"
            ],
            "title": "Embedder",
            "type": "string"
          },
          "truncate_dim": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Truncate Dim"
          },
          "metric": {
            "default": "cosine",
            "enum": [
              "cosine",
              "l2"
            ],
            "title": "Metric",
            "type": "string"
          },
          "build_fts": {
            "default": true,
            "title": "Build Fts",
            "type": "boolean"
          }
        },
        "title": "LanceDbIndexConfig",
        "type": "object"
      },
      "learn": {}
    }
  },
  "retrieve": {
    "dense": {
      "name": "dense",
      "version": "1",
      "summary": "Turns the question into a vector with the same model the index used, and returns the pieces whose vectors are closest. It matches meaning, so it finds paraphrases, but it can miss exact names, codes and rare words.",
      "stage": "retrieve",
      "output": "retrieval_result",
      "stackable": false,
      "deterministic": true,
      "cacheable": true,
      "requires": {
        "index": {
          "backends": [
            "dense"
          ]
        }
      },
      "provides": {},
      "inputs": {
        "index": {
          "type": "index",
          "variadic": false,
          "ambient": false,
          "required": true
        },
        "query": {
          "type": "query",
          "variadic": false,
          "ambient": true,
          "required": true
        }
      },
      "config_schema": {
        "properties": {
          "top_k": {
            "default": 20,
            "title": "Top K",
            "type": "integer"
          }
        },
        "title": "DenseConfig",
        "type": "object"
      },
      "learn": {}
    },
    "bm25": {
      "name": "bm25",
      "version": "1",
      "summary": "Scores pieces by the words they share with the question, counting rare words for more than common ones (the BM25 formula). It finds exact names, codes and terms that vector search can miss, but it does not know that different words can mean the same thing.",
      "stage": "retrieve",
      "output": "retrieval_result",
      "stackable": false,
      "deterministic": true,
      "cacheable": true,
      "requires": {
        "index": {
          "backends": [
            "fts"
          ]
        }
      },
      "provides": {},
      "inputs": {
        "index": {
          "type": "index",
          "variadic": false,
          "ambient": false,
          "required": true
        },
        "query": {
          "type": "query",
          "variadic": false,
          "ambient": true,
          "required": true
        }
      },
      "config_schema": {
        "properties": {
          "top_k": {
            "default": 20,
            "title": "Top K",
            "type": "integer"
          }
        },
        "title": "Bm25Config",
        "type": "object"
      },
      "learn": {}
    },
    "hybrid_rrf": {
      "name": "hybrid_rrf",
      "version": "1",
      "summary": "Runs vector search and keyword search side by side, then merges the two ranked lists by position rather than by score (reciprocal rank fusion). A piece near the top of both lists wins, so it gets the strengths of both searches.",
      "stage": "retrieve",
      "output": "retrieval_result",
      "stackable": false,
      "deterministic": true,
      "cacheable": true,
      "requires": {
        "index": {
          "backends": [
            "dense",
            "fts"
          ]
        }
      },
      "provides": {},
      "inputs": {
        "index": {
          "type": "index",
          "variadic": false,
          "ambient": false,
          "required": true
        },
        "query": {
          "type": "query",
          "variadic": false,
          "ambient": true,
          "required": true
        }
      },
      "config_schema": {
        "properties": {
          "top_k": {
            "default": 20,
            "title": "Top K",
            "type": "integer"
          },
          "rrf_k": {
            "default": 60,
            "title": "Rrf K",
            "type": "integer"
          }
        },
        "title": "HybridRrfConfig",
        "type": "object"
      },
      "learn": {}
    }
  },
  "rerank": {
    "mmr": {
      "name": "mmr",
      "version": "1",
      "summary": "Maximal Marginal Relevance picks results one at a time, each time taking the piece that best matches the question while being least like the pieces already picked. It trades a little relevance for variety, and it reuses the index's own vectors, so it needs no extra model.",
      "stage": "rerank",
      "output": "retrieval_result",
      "stackable": true,
      "deterministic": true,
      "cacheable": true,
      "requires": {},
      "provides": {},
      "inputs": {
        "result": {
          "type": "retrieval_result",
          "variadic": false,
          "ambient": false,
          "required": true
        },
        "query": {
          "type": "query",
          "variadic": false,
          "ambient": true,
          "required": true
        },
        "index": {
          "type": "index",
          "variadic": false,
          "ambient": true,
          "required": true
        }
      },
      "config_schema": {
        "properties": {
          "lambda_mult": {
            "default": 0.5,
            "title": "Lambda Mult",
            "type": "number"
          },
          "top_k": {
            "default": 5,
            "title": "Top K",
            "type": "integer"
          }
        },
        "title": "MmrRerankConfig",
        "type": "object"
      },
      "learn": {}
    }
  }
}
