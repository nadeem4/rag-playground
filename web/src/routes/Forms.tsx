// Owned by the schema-form task (B2). Replace this placeholder with a gallery
// that renders every registered plugin's config form from registry.json.
export function Forms() {
  return (
    <main className="p-4 text-sm text-muted-foreground">
      Form gallery not built yet.
    </main>
  )
}
