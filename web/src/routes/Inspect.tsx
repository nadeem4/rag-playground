// Owned by the inspector task (B4). Replace this placeholder with the inspector
// gallery rendered against the real-engine fixtures.
export function Inspect() {
  return (
    <main className="p-4 text-sm text-muted-foreground">
      Inspector gallery not built yet.
    </main>
  )
}
